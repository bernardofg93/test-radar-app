import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
   container: {
      padding: 10,
      backgroundColor: '#000',
      color: '#fff',
      marginBottom: 10,
      borderRadius: 20
   },
   title: {
      color: '#fff',
      fontSize: 15,
      textAlign: 'center',
      fontWeight: 'bold'
   }
})
export default styles;
